# Morpheus AGI Chatbot Framework - Backend Dependencies

# FastAPI and ASGI server
fastapi==0.104.1
uvicorn[standard]==0.24.0

# LangChain and OpenAI integration (latest versions)
langchain==0.1.0
langchain-openai==0.0.2
langchain-community==0.0.13
openai>=1.6.1,<2.0.0

# Telemetry (V2.4.2)
# Pin to v2 API and keep anyio<4 for FastAPI 0.104 compatibility
langfuse==2.39.0
anyio<4

# Data validation and serialization
pydantic==2.5.0
pydantic-settings==2.1.0

# HTTP client for external API calls
httpx==0.25.2

# Environment variable management
python-dotenv==1.0.0

# CORS middleware
python-multipart==0.0.6

# Logging and monitoring
structlog==23.2.0

# Testing dependencies
pytest==7.4.3
pytest-asyncio==0.21.1

# Development dependencies
black==23.11.0
flake8==6.1.0
isort==5.12.0

# Future dependencies (for V2-V4)
faiss-cpu==1.8.0
tiktoken==0.5.2
filelock==3.16.0

# Database support (V2)
sqlmodel==0.0.16
alembic==1.13.1

# PDF parsing (V2)
pypdf==4.3.1

# Background task processing (V3)
# celery==5.3.4
# redis==5.0.1
