# timestamp: 2026-03-28T14-40-36
# project_id: html-import:Dreaming When You Sleep - DELTA-A.4.4 Finalization
# model: gpt-5.4-mini
# success: true

====== TAGGER PROMPT (SYSTEM) ======
You are a memory tagging system.

You are a memory tagging system.

Your task is to extract compact, durable metadata for later retrieval.
Tag the CURRENT TURN only.

Use PREVIOUS TURN content only as supporting context to interpret the CURRENT TURN correctly.
Do not tag the PREVIOUS TURN as if it were the current exchange.
The goal is to help future searches reliably find the CURRENT TURN and its most durable meaning.

Use ONLY the content provided.
Do NOT invent information.
Do NOT infer long term meaning beyond what is explicit.

Prefer tags that:
- Would plausibly be used as search queries later
- Represent concrete concepts, entities, or themes
- Are stable over time

If present, extract:
- Proper names (people, organizations, projects, locations, technologies)
- Clearly stated topics or subject matter
- The user’s intent in asking the question
- The general category of the exchange

Guidelines:
- Use 3 to 7 topics maximum
- Topics should be lowercase, comma separated keywords
- Include proper names exactly as written
- Do not include filler words or generic terms
- Avoid one off or throwaway details

Intent should describe the purpose of the exchange, for example:
- explore story ideas
- ask for explanation
- design a system
- evaluate consequences
- clarify a concept

Type should be one or two broad categories such as:
technical, story, design, system, research, planning, meta

Semantic handle:
- A short, human readable noun phrase that names what this exchange is about
- Should be understandable without surrounding conversation
- Should avoid pronouns like this, that, it, we
- Should not be a full sentence
- Prefer 5 to 12 words
- Do not include commentary or explanation

Open questions:
- Identify only high-value unresolved questions from this exchange.
- Extract question candidates from the ASSISTANT response text only.
- Do NOT extract questions from USER prompt text, even if the user asks a direct question.
- Do NOT extract routine brainstorming branches, minor follow-up ideas, casual suggestions, or low-stakes uncertainty.
- Only include a question if it is likely to matter later for research, design quality, major decisions, or long-term project direction.
- Prefer zero questions over weak questions.

Inclusion standard:
- Include a question only if at least one of these is true:
  - answering it would likely require outside research or verification
  - answering it would materially improve a major design, story, or project decision
  - it represents a genuinely important unresolved dependency or blocker
- Exclude questions that are:
  - already effectively resolved in the same exchange
  - simple next-step ideas
  - optional creative variations
  - minor style or wording choices
  - speculative branches that do not clearly matter

Question handling:
- Include explicit ASSISTANT-side questions ending with '?' only if they remain genuinely unresolved and important.
- Include implicit unresolved questions only when they are clearly important and durable, such as:
  - major unresolved design choices
  - critical pending decisions
  - research questions that need evidence
  - blockers or dependencies that affect later work

Resolution values:
- `remind_user` when a real user decision is required and it is important enough to revisit
- `answer_local` when it is important and can likely be answered from project/local context
- `answer_remote` when it is important and likely requires external research

Limits:
- It is valid and preferred to return an empty `questions` array.
- Do not emit weak, resolved, or low-value questions.

Return STRICT JSON only. No prose. No markdown fences.

Schema:
{
  "topics": "",
  "intent": "",
  "type": "",
  "semantic_handle": "",
  "questions": [
    {
      "question": "<exact or naturally rewritten question>",
      "topic": "<topic title where the question originated>",
      "resolution": "<remind_user | answer_local | answer_remote>"
    }
  ]
}

Rules:
- Always include all keys shown in the schema.
- Use "" (empty string) for unknown values. Do not output null.
- `questions` MUST be an array (use [] when no candidates).
- `resolution` MUST be one of: ignore, remind_user, answer_local, answer_remote.


====== TAGGER PROMPT (USER) ======
------PREVIOUS TURN------
------PREVIOUS TAGS------
#route: other
#keep: false
#topics: Morpheus, DELTA-A.4.3, DELTA-A.4.4, RAG, prompt assembly contracts, candidate selection, provenance locking
#intent: continue system design specification
#type: technical, design
#semantic_handle: Morpheus A.4 retrieval selection pipeline

------USER------
I want to continue to develop the Morpheus Delta A.4 series of changes, 4.2 is done, and below is the summary of A.3.

DELTA-A.4.3 — Candidate Selection and Context Pruning (Summary)
Purpose A.4.3 introduces the first stage where retrieved RAG candidates may be dropped. Its goal is to select a high-quality, bounded set of snippets to include in the final prompt context, after candidates have already been unified (A.4.1) and ordered by relevance (A.4.2).
Key Principles
* A.4.3 operates after deterministic ordering by similarity score.
* Selection is policy-driven, not model-driven.
* This stage is responsible for enforcing token budgets and relevance cutoffs.
* No semantic inference or additional LLM calls are introduced.
Core Behaviors
* Candidates are considered in ranked order (highest similarity first).
* The system MAY:
    * Keep only the top N candidates.
    * Stop adding candidates once a token budget is reached.
    * Drop candidates below a similarity threshold.
* The system MUST:
    * Preserve the highest-scoring candidates whenever possible.
    * Prefer fewer strong snippets over many weak ones.
Signals Allowed (Optional, Non-Required)
* Similarity score (primary signal).
* Source metadata (Daily vs LTM), if explicitly encoded as policy.
* Timestamp / recency, only if available as first-class metadata.
Signals Explicitly Deferred or Forbidden
* No model-based judgment.
* No semantic re-ranking.
* No parsing of timestamps or tags from raw text.
* No guessing or heuristic inference.
Failure and Edge Handling
* If only low-confidence candidates exist, the system MAY include a small number and mark uncertainty.
* Absence of metadata MUST NOT block selection; the system falls back to score-only behavior.
Invariants
* Higher-ranked candidates are never dropped in favor of lower-ranked ones.
* Selection behavior is deterministic and reproducible.
* A.4.3 does not alter candidate ordering; it only truncates the list.
Mental Model
* A.4.1: What could be relevant?
* A.4.2: What is more relevant?
* A.4.3: What do we actually keep?

------ASSISTANT------
Good, this is a clean place to continue. You are at exactly the right abstraction level for A.4.4.You have A.4.3 defined as 
deterministic truncation and pruning after ranking. The next step should
 finish the “selection contract” so downstream stages never have to 
reason about uncertainty, overflow, or provenance again.Below is a proposedDELTA-A.4.4that builds directly on your A.4.3 invariants and closes the loop before prompt assembly.DELTA-A.4.4 — Selection Finalization and Prov
...[middle omitted for tagging]...
n as an auditable system, not magicIt is also the clean seam where future “dream analysis” can observe behavior without influencing it.If you want, the next natural step after this isA.5 Prompt Assembly Contracts, where we define rules like:• Context is append-only• Instruction layers cannot overwrite memory• Memory never contains instructionsThat is where Morpheus really starts to feel like an engineered cognitive system instead of a RAG pipeline.Say the word and we will draft A.5.

------CURRENT TURN------
------USER------
ok, we need to do 4.3, that was just to get this chat framed correctly.

So, what are we thinking for rules to remove snippets in 4.3?

------ASSISTANT------
Perfect, thanks for the reset. Staying strictly insideA.4.3.What follows arepolicy rules only. No model judgment, no inference, no magic. Everything is deterministic, inspectable, and cheap.I will frame this asremoval rules, not selection rules, since A.4.2 already gave us an ordered list.A.4.3 — Snippet Removal RulesDesign GoalRemove low-value candidates whilenever harming the best ones. The system should bias toward a small number of strong snippets and stop early whenever possible.A.4.3 answers one question only:What must be dropped before prompt assembly?Rule Set (Applied in Order)Rule 1: Hard Similarity FloorDrop any snippet with similarity < MIN_SCORE• Applies globally• Deterministic• Optional, configurable• Disabled if MIN_SCORE = 0Rationale:This prevents garbage recall from polluting context when retrieval returns weak matches.Invariant:
...[middle omitted for tagging]...
irst-class metadata• Remove based on downstream prompt needsThis stage is blind to meaning.Default Minimal Policy (Recommended Starting Point)If you want a clean first implementation:• MIN_SCORE = 0• Top-N = 6• TOKEN_BUDGET = enforced• No weak-tail truncation• No source capsThis yields:Predictable behaviorEasy debuggingStrong bias toward high-quality recallA.4.3 Output ContractThe output of A.4.3 MUST:• Preserve original ordering• Contain only kept snippets• Include no annotations or reasoning• Be fully reproducible from inputs + policyMental Model (Restated)A.4.3 is aguillotine, not a judge.It does not decide what is good.It only enforces limits.If you want, next we can:• Turn this into a formal policy table• Define exact config knobs and defaults• Walk through real retrieval examples and see what survivesJust tell me where you want to go next.


====== TAGGER RESPONSE ======
{
  "topics": "Morpheus, DELTA-A.4.3, snippet removal, candidate selection, token budget, similarity floor",
  "intent": "ask for rules to remove snippets in A.4.3",
  "type": "technical, design",
  "semantic_handle": "A.4.3 snippet removal rules",
  "questions": []
}
