# timestamp: 2026-03-28T14-16-32
# project_id: html-import:Dreaming When You Sleep - Dream Agent Design
# model: gpt-5.4-mini
# success: true

====== TAGGER PROMPT (SYSTEM) ======
You are a memory tagging system.

You are a memory tagging system.

Your task is to extract compact, durable metadata for later retrieval.
Tag the CURRENT TURN only.

Use PREVIOUS TURN content only as supporting context to interpret the CURRENT TURN correctly.
Do not tag the PREVIOUS TURN as if it were the current exchange.
The goal is to help future searches reliably find the CURRENT TURN and its most durable meaning.

Use ONLY the content provided.
Do NOT invent information.
Do NOT infer long term meaning beyond what is explicit.

Prefer tags that:
- Would plausibly be used as search queries later
- Represent concrete concepts, entities, or themes
- Are stable over time

If present, extract:
- Proper names (people, organizations, projects, locations, technologies)
- Clearly stated topics or subject matter
- The user’s intent in asking the question
- The general category of the exchange

Guidelines:
- Use 3 to 7 topics maximum
- Topics should be lowercase, comma separated keywords
- Include proper names exactly as written
- Do not include filler words or generic terms
- Avoid one off or throwaway details

Intent should describe the purpose of the exchange, for example:
- explore story ideas
- ask for explanation
- design a system
- evaluate consequences
- clarify a concept

Type should be one or two broad categories such as:
technical, story, design, system, research, planning, meta

Semantic handle:
- A short, human readable noun phrase that names what this exchange is about
- Should be understandable without surrounding conversation
- Should avoid pronouns like this, that, it, we
- Should not be a full sentence
- Prefer 5 to 12 words
- Do not include commentary or explanation

Open questions:
- Identify only high-value unresolved questions from this exchange.
- Extract question candidates from the ASSISTANT response text only.
- Do NOT extract questions from USER prompt text, even if the user asks a direct question.
- Do NOT extract routine brainstorming branches, minor follow-up ideas, casual suggestions, or low-stakes uncertainty.
- Only include a question if it is likely to matter later for research, design quality, major decisions, or long-term project direction.
- Prefer zero questions over weak questions.

Inclusion standard:
- Include a question only if at least one of these is true:
  - answering it would likely require outside research or verification
  - answering it would materially improve a major design, story, or project decision
  - it represents a genuinely important unresolved dependency or blocker
- Exclude questions that are:
  - already effectively resolved in the same exchange
  - simple next-step ideas
  - optional creative variations
  - minor style or wording choices
  - speculative branches that do not clearly matter

Question handling:
- Include explicit ASSISTANT-side questions ending with '?' only if they remain genuinely unresolved and important.
- Include implicit unresolved questions only when they are clearly important and durable, such as:
  - major unresolved design choices
  - critical pending decisions
  - research questions that need evidence
  - blockers or dependencies that affect later work

Resolution values:
- `remind_user` when a real user decision is required and it is important enough to revisit
- `answer_local` when it is important and can likely be answered from project/local context
- `answer_remote` when it is important and likely requires external research

Limits:
- It is valid and preferred to return an empty `questions` array.
- Do not emit weak, resolved, or low-value questions.

Return STRICT JSON only. No prose. No markdown fences.

Schema:
{
  "topics": "",
  "intent": "",
  "type": "",
  "semantic_handle": "",
  "questions": [
    {
      "question": "<exact or naturally rewritten question>",
      "topic": "<topic title where the question originated>",
      "resolution": "<remind_user | answer_local | answer_remote>"
    }
  ]
}

Rules:
- Always include all keys shown in the schema.
- Use "" (empty string) for unknown values. Do not output null.
- `questions` MUST be an array (use [] when no candidates).
- `resolution` MUST be one of: ignore, remind_user, answer_local, answer_remote.


====== TAGGER PROMPT (USER) ======
------PREVIOUS TURN------
------PREVIOUS TAGS------
#route: other
#keep: false
#topics: Researcher Agent, prompt template, JSON input, Python parameters, run_research_agent
#intent: tweak research agent prompt and clarify input structure
#type: technical, design
#semantic_handle: research agent prompt and parameter input format

------USER------
this was our prompt, we need to tweak it, and also, do we need to pass in json?  or can we just have the parameters put the values in?
Researcher Agent Prompt (One Topic Per Call)

You are the Researcher Agent of the Morpheus Dream Cycle.
You receive a single research task that originated from a DreamEntry created by the Idea Agent.

You will receive a JSON object with this structure:

{
"entry_id": "...",
"origin_text": "...",
"origin_type": "...",
"assistant_response": "...",
"theme": "...",
"research_topic": "..."
}

Your Tasks:

Understand the context of the research request by reading origin_text, origin_type, and assistant_response.
This gives you the reason for the research.

Perform a research query using the system Research tool.
Use the exact research_topic string provided.

Summarize the research results.
Your research_summary must:

be concise and clear

relate directly to the origin_text and assistant_response

avoid hallucinated citations

avoid fabricated papers or URLs

not include tool citation artifacts such as
...
or oaicite

use retrieved content when available

fall back to general domain knowledge when necessary

Confidence scoring:

0.80 if results strongly match retrieved content

0.55 if partially grounded

0.30 if weakly grounded

Return a Research Entry JSON object:

{
"entry_id": "<same entry_id>",
"agent": "researcher_agent",
"timestamp": "<ISO timestamp>",
"research_topic": "<same research_topic>",
"research_summary": "<your summary>",
"metadata": {
"confidence": <score>,
"related_theme": "<same theme>"
}
}

Global Rules:
Never summarize dream_json.
Never modify or reinterpret the Idea Agent’s assistant_response.
Never generate opinions or insights. Only provide factual background.
Never generate insights or opinions. Only produce research summaries.
Never include explanation outside JSON in the final output.
Never output anything except the JSON object.
Never include explanation outside JSON.
Never hallucinate specific papers, authors, or URLs.
You may reference well known concepts if accurate.
Use the research tool whenever available.
Never include tool citation markers such as :contentReference[...] or oaicite. 
Remove all citation artifacts from research_summary.
Do not repeat origin_text or assistant_response in your research_summary.
Your role is to supplement the insight, not restate it.
If you are unsure of a specific model or theory name, omit the parentheses entirely instead of leaving them empty.


Final Instruction:
Produce only the JSON object described above. Nothing else.

research_json:
{research_json}

------ASSISTANT------
Here is a clean, Cursor friendly rewrite of the Research Agent prompt with two goals:Remove all unnecessary wording and make it consistent with your architecture.Remove the requirement to pass JSON into the prompt (you are right, you do not need to).Instead, pass each field directly as parameters in the prompt template.No em dashes.Clean, compact, deterministic.Ready for research_prompts.py.Rewritten Research Agent Prompt (Parameter Based, Not JSON Input)Below 
...[middle omitted for tagging]...
 only the fields it needs.4. Much less brittleThe old version:forced the model to read an embedded JSONduplicated keysincreased hallucination riskconfused error reportingThis new version eliminates all that.If you want nextI can produce:the Python function for research_prompts.pythe integration code for run_research_agentthe merge layer requirement updatesthe cleaned Idea Agent prompt to match this styleJust tell me:Generate the research prompt builder function.

------CURRENT TURN------
------USER------
I had it make the file here is the call we can reference:
def build_research_prompt(
    project_summary_text: str,
    origin_text: str,
    origin_type: str,
    assistant_response: str,
    research_topic: str,
    theme: str,
    entry_id: str,
) -> str:

------ASSISTANT------
Perfect.This signature is exactly what we want.It is clean, explicit, and prevents any ambiguity about what the Research Agent receives.Here is thefinal Research Agent prompt, rewritten with your exact parameter list so it maps perfectly to:scssCopy codebuild_research_prompt(
    project_summary_text,
    origin_text,
    origin_type,
    assistant_response,
    research_topic,
    theme,
    entry_id
)This is the version you should paste intoresearch_prompts.py.No em dashes.Final Research Agent Prompt (matches your function signature)cssCopy codeYou are the Researcher Agent for the M
...[middle omitted for tagging]...
mestamp>","research_topic":"{research_topic}","research_summary":"<your summary>","metadata": {
    "confidence": <score>,"related_theme":"{theme}"}
}

Global Rules:
- Output only the JSON object and nothing else.  
- Do not modify the Idea Agent response.  
- Do not generate new insights.  
- Do not mention the research tool.  
- If unsure about a specific theory name, describe it without naming it.Next Step (optional)If you want, I can produce the exactbuild_research_promptfunction implementation that injects these values safely. Just say:Generate the build_research_prompt function.


====== TAGGER RESPONSE ======
{
  "topics": "build_research_prompt, research_prompts.py, function signature, parameters, prompt template",
  "intent": "clarify prompt input structure",
  "type": "technical, design",
  "semantic_handle": "research prompt function signature",
  "questions": []
}
