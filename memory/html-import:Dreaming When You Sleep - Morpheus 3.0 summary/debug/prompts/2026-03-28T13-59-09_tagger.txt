# timestamp: 2026-03-28T13-59-09
# project_id: html-import:Dreaming When You Sleep - Morpheus 3.0 summary
# model: gpt-5.4-mini
# success: true

====== TAGGER PROMPT (SYSTEM) ======
You are a memory tagging system.

You are a memory tagging system.

Your task is to extract compact, durable metadata for later retrieval.
Tag the CURRENT TURN only.

Use PREVIOUS TURN content only as supporting context to interpret the CURRENT TURN correctly.
Do not tag the PREVIOUS TURN as if it were the current exchange.
The goal is to help future searches reliably find the CURRENT TURN and its most durable meaning.

Use ONLY the content provided.
Do NOT invent information.
Do NOT infer long term meaning beyond what is explicit.

Prefer tags that:
- Would plausibly be used as search queries later
- Represent concrete concepts, entities, or themes
- Are stable over time

If present, extract:
- Proper names (people, organizations, projects, locations, technologies)
- Clearly stated topics or subject matter
- The user’s intent in asking the question
- The general category of the exchange

Guidelines:
- Use 3 to 7 topics maximum
- Topics should be lowercase, comma separated keywords
- Include proper names exactly as written
- Do not include filler words or generic terms
- Avoid one off or throwaway details

Intent should describe the purpose of the exchange, for example:
- explore story ideas
- ask for explanation
- design a system
- evaluate consequences
- clarify a concept

Type should be one or two broad categories such as:
technical, story, design, system, research, planning, meta

Semantic handle:
- A short, human readable noun phrase that names what this exchange is about
- Should be understandable without surrounding conversation
- Should avoid pronouns like this, that, it, we
- Should not be a full sentence
- Prefer 5 to 12 words
- Do not include commentary or explanation

Open questions:
- Identify only high-value unresolved questions from this exchange.
- Extract question candidates from the ASSISTANT response text only.
- Do NOT extract questions from USER prompt text, even if the user asks a direct question.
- Do NOT extract routine brainstorming branches, minor follow-up ideas, casual suggestions, or low-stakes uncertainty.
- Only include a question if it is likely to matter later for research, design quality, major decisions, or long-term project direction.
- Prefer zero questions over weak questions.

Inclusion standard:
- Include a question only if at least one of these is true:
  - answering it would likely require outside research or verification
  - answering it would materially improve a major design, story, or project decision
  - it represents a genuinely important unresolved dependency or blocker
- Exclude questions that are:
  - already effectively resolved in the same exchange
  - simple next-step ideas
  - optional creative variations
  - minor style or wording choices
  - speculative branches that do not clearly matter

Question handling:
- Include explicit ASSISTANT-side questions ending with '?' only if they remain genuinely unresolved and important.
- Include implicit unresolved questions only when they are clearly important and durable, such as:
  - major unresolved design choices
  - critical pending decisions
  - research questions that need evidence
  - blockers or dependencies that affect later work

Resolution values:
- `remind_user` when a real user decision is required and it is important enough to revisit
- `answer_local` when it is important and can likely be answered from project/local context
- `answer_remote` when it is important and likely requires external research

Limits:
- It is valid and preferred to return an empty `questions` array.
- Do not emit weak, resolved, or low-value questions.

Return STRICT JSON only. No prose. No markdown fences.

Schema:
{
  "topics": "",
  "intent": "",
  "type": "",
  "semantic_handle": "",
  "questions": [
    {
      "question": "<exact or naturally rewritten question>",
      "topic": "<topic title where the question originated>",
      "resolution": "<remind_user | answer_local | answer_remote>"
    }
  ]
}

Rules:
- Always include all keys shown in the schema.
- Use "" (empty string) for unknown values. Do not output null.
- `questions` MUST be an array (use [] when no candidates).
- `resolution` MUST be one of: ignore, remind_user, answer_local, answer_remote.


====== TAGGER PROMPT (USER) ======
------PREVIOUS TURN------
------PREVIOUS TAGS------
#route: other
#keep: false
#topics: Morpheus 3.0, sleep cycle, pruning prompt, formatting prompt, chat log summarization, FR-3.0.4
#intent: confirm a two-step prompt pipeline for log pruning and formatting
#type: technical, planning
#semantic_handle: two-step pruning and formatting prompt pipeline

------USER------
It looks like the earlier extractor that prunes, did it in a two step process, with these prompts.  I think we want to start with them, since we know they worked pretty well.

def generate_pruning_prompt(content: str) -> str:
    """Generate a prompt for pruning a chat log."""
    logger.info("Generating pruning prompt")
    return f"""You are given a chat log with alternating USER and ASSISTANT messages.

Your task:

1. **PRESERVE ALL HEADER LINES**: Keep all lines that start with # 
    (like #created:, #tokens:, etc.) exactly as they are.
2. Remove all chatty filler, greetings, and follow-up offers from assistant responses.
3. For each user question, keep only the substantive answer in the assistant's 
    response—**do not repeat, paraphrase, or echo the user's question in the answer.** 
    If the assistant's response starts by restating or rewording the user's question, 
    remove that redundant intro and keep only the real answer, facts, logic, or decision.
4. Do not alter the user's original prompts.
5. Keep all assistant responses that contain explanations, facts, or decisions, as 
   long as they are not just repeats of the question.
6. Keep the timestamps at the bottom of every response.
7. If multiple Q&As can be summarized even more tightly (e.g., several closely related 
   questions with short answers), you may combine them into one concise answer, as long 
   as nothing important is lost.
8. **PRESERVE THE REFERENCES SECTION**: Keep the "=== REFERENCES ===" section and 
   all URLs exactly as they are.

Do not include any meta-commentary or explanations—just the pruned and 
compressed log with preserved headers and references.

Here is the chat log:
{content}

"""


def generate_formatting_prompt(content: str) -> str:
    """Generate a prompt to structure a pruned chat log into topics/metadata with normalized 
       headings and context awareness."""
    logger.info("Generating formatting prompt")
    return f"""You are given a pruned chat log with alternating USER and ASSISTANT messages.

Your task is to organize it into structured sections for easier retrieval:

1. Group related Q&A exchanges under topic sections. Insert a heading line in this format:
   === TOPIC: normalized short title ===
   - Keep titles concise and consistent across the file.
   - Standardize headings that use different words for the same concept 
     (e.g. "Lesson 4", "Class 4", "Session 4") → normalize to "Lesson 4".
   - Use consistent casing: Title Case, no extra punctuation.

2. Immediately after each heading, insert metadata lines:
   - #topics: 3–5 keywords capturing the main ideas
   - #decisions: any explicit choices or conclusions made (leave blank if none)
   - #open_questions: unresolved issues or next steps (leave blank if none)
   - #user_context: include only if the user’s background 
      (e.g. software engineer, AI/AGI focus, fiction writing)
     directly affects or is relevant to this section. Otherwise, leave blank.
   - Inline: where a recurring theme (e.g. RAG, pruning, sleep cycle) 
      is explicitly discussed, insert a [tag:...] marker in that 
      sentence to strengthen retrieval.

3. Keep all USER and ASSISTANT messages inside each topic section exactly as 
   they appear in the pruned log. Do not compress them further.

4. Preserve all header lines at the very top (e.g. #created, #tokens, etc.) and the 
   REFERENCES section at the end, if present.

5. At the very end of the file, create two appendices:
   [Decisions Log]
   - Collect and deduplicate all #decisions from across the file.
   [Open Questions]
   - Collect and deduplicate all #open_questions from across the file.

6. Do not add commentary, explanations, or closing statements. 
 Only return the structured log with topic headings, 
   metadata, and the original pruned Q&A grouped inside.

Here is the chat log:
{content}
"""

------ASSISTANT------
Yes — that’s exactly the pair we want to start 3.0’s pruning process with. Those two prompts already perform thecore “sleep” cyclefunctions in sequence:Pruning Step (generate_pruning_prompt)Clea
...[middle omitted for tagging]...
and tuned later for Dreaming (4.0).Would you like me to draft the 
exact functional requirements for the summarization part (FR-3.0.4) 
using this two-step prompt pipeline?[11-09-2025 23:58 EST]

------CURRENT TURN------
------USER------
oh, about verify RAG.  from my thinking, we will move pruned.txt to memory/[project]/uploads, then re-running the build process.  Or is there a way to add to a RAG after the fact?

------ASSISTANT------
That’s
 a key design choice — and both paths are viable depending on how 
tightly you want the Sleep Cycle integrated with the RAG builder.Here’s the breakdown:Option A — Movepruned.txt→uploadsand rebuild RAG (simpler, cleaner)ProcessSleep Cycle generatesmemory/{project}/pruned.txt.Move it tomemory/{project}/uploads/assleep_summary.txtor similar.Trigger your existingrebuild_rag(project_id)pipeline (same one used when user uploads files).Pros
...[middle omitted for tagging]...
ep Cycle, go withOption A— rebuild from the newpruned.txtmoved intouploads.It’s:Deterministic (reproducible each night).Compatible with all your route-aware and namespace systems.Easily verified via/rag/verify.Later (3.5+), you can extend withincremental merge modeonce nightly rebuilds are stable.If you’d like, I can write out the newFR-3.0.8 — RAG Rebuild from Pruned Outputsection to append to the 3.0 requirements set.[11-10-2025 00:05 EST]


====== TAGGER RESPONSE ======
{
  "topics": "rag verification, pruned.txt, memory uploads, rebuild_rag, incremental merge mode, Sleep Cycle",
  "intent": "evaluate how to update rag from pruned output",
  "type": "technical, design",
  "semantic_handle": "rag rebuild from pruned output",
  "questions": [
    {
      "question": "Is there a way to add to a RAG after the fact?",
      "topic": "rag verification",
      "resolution": "answer_remote"
    }
  ]
}
