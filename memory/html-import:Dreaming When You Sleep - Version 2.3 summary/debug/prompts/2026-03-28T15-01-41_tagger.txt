# timestamp: 2026-03-28T15-01-41
# project_id: html-import:Dreaming When You Sleep - Version 2.3 summary
# model: gpt-5.4-mini
# success: true

====== TAGGER PROMPT (SYSTEM) ======
You are a memory tagging system.

You are a memory tagging system.

Your task is to extract compact, durable metadata for later retrieval.
Tag the CURRENT TURN only.

Use PREVIOUS TURN content only as supporting context to interpret the CURRENT TURN correctly.
Do not tag the PREVIOUS TURN as if it were the current exchange.
The goal is to help future searches reliably find the CURRENT TURN and its most durable meaning.

Use ONLY the content provided.
Do NOT invent information.
Do NOT infer long term meaning beyond what is explicit.

Prefer tags that:
- Would plausibly be used as search queries later
- Represent concrete concepts, entities, or themes
- Are stable over time

If present, extract:
- Proper names (people, organizations, projects, locations, technologies)
- Clearly stated topics or subject matter
- The user’s intent in asking the question
- The general category of the exchange

Guidelines:
- Use 3 to 7 topics maximum
- Topics should be lowercase, comma separated keywords
- Include proper names exactly as written
- Do not include filler words or generic terms
- Avoid one off or throwaway details

Intent should describe the purpose of the exchange, for example:
- explore story ideas
- ask for explanation
- design a system
- evaluate consequences
- clarify a concept

Type should be one or two broad categories such as:
technical, story, design, system, research, planning, meta

Semantic handle:
- A short, human readable noun phrase that names what this exchange is about
- Should be understandable without surrounding conversation
- Should avoid pronouns like this, that, it, we
- Should not be a full sentence
- Prefer 5 to 12 words
- Do not include commentary or explanation

Open questions:
- Identify only high-value unresolved questions from this exchange.
- Extract question candidates from the ASSISTANT response text only.
- Do NOT extract questions from USER prompt text, even if the user asks a direct question.
- Do NOT extract routine brainstorming branches, minor follow-up ideas, casual suggestions, or low-stakes uncertainty.
- Only include a question if it is likely to matter later for research, design quality, major decisions, or long-term project direction.
- Prefer zero questions over weak questions.

Inclusion standard:
- Include a question only if at least one of these is true:
  - answering it would likely require outside research or verification
  - answering it would materially improve a major design, story, or project decision
  - it represents a genuinely important unresolved dependency or blocker
- Exclude questions that are:
  - already effectively resolved in the same exchange
  - simple next-step ideas
  - optional creative variations
  - minor style or wording choices
  - speculative branches that do not clearly matter

Question handling:
- Include explicit ASSISTANT-side questions ending with '?' only if they remain genuinely unresolved and important.
- Include implicit unresolved questions only when they are clearly important and durable, such as:
  - major unresolved design choices
  - critical pending decisions
  - research questions that need evidence
  - blockers or dependencies that affect later work

Resolution values:
- `remind_user` when a real user decision is required and it is important enough to revisit
- `answer_local` when it is important and can likely be answered from project/local context
- `answer_remote` when it is important and likely requires external research

Limits:
- It is valid and preferred to return an empty `questions` array.
- Do not emit weak, resolved, or low-value questions.

Return STRICT JSON only. No prose. No markdown fences.

Schema:
{
  "topics": "",
  "intent": "",
  "type": "",
  "semantic_handle": "",
  "questions": [
    {
      "question": "<exact or naturally rewritten question>",
      "topic": "<topic title where the question originated>",
      "resolution": "<remind_user | answer_local | answer_remote>"
    }
  ]
}

Rules:
- Always include all keys shown in the schema.
- Use "" (empty string) for unknown values. Do not output null.
- `questions` MUST be an array (use [] when no candidates).
- `resolution` MUST be one of: ignore, remind_user, answer_local, answer_remote.


====== TAGGER PROMPT (USER) ======
------PREVIOUS TURN------
------PREVIOUS TAGS------
#route: other
#keep: false
#topics: rag, query builder, router, mini model, builder_llm, chitchat, json schema
#intent: design a pre-rag query builder and router that decides whether to retrieve and how to rewrite queries
#type: technical, design
#semantic_handle: RAG query builder and router requirement

------USER------
ok, 2.3 seems to be working.  I have a change I want to make as 2.3.1 though.  I was kind of chatting with morpheus as a way to see it rolling off the conversations and noticed that the this is what the RAG query looked like:
2025-10-16 13:31:29,226 - DEBUG - app.core.rag_manager:retrieve_context - RAG: query='how do people generally implement this?  a quick prompt(s) to a mini model to make the rag prompt?...' top_k=10 got 10 results (pre-filter)

so, as I was making it rolled over I was asking about how to improve the RAG query, and kind of came up with this:

how do people generally implement this?  a quick prompt(s) to a mini model to make the rag prompt?
Short answer: yes—most teams use one small LLM call (sometimes two) to build a compact “query bundle” for RAG: route, rewritten standalone query, a few paraphrases, and optional HyDE. Others use rules for routing and a small LLM only for rewriting.

Common implementation patterns
- Single-call builder (most common): One small instruct model returns JSON with route + standalone query + paraphrases + HyDE + entities. Simple and fast.
- Two-stage (router → rewriter): Cheap rule/ML router first; only call the small LLM to rewrite when RAG is needed.
- Rule-first fallback: Regex/NER and heuristics handle 70–90% of turns; small LLM is a fallback for ambiguous queries.
- Offline doc expansion: Use SPLADE/doc2query to expand documents; at query time a tiny LLM just normalizes the user turn.

Typical budgets
- Prompt tokens: 100–300 (includes a 1–2 sentence history summary).
- Output tokens: 50–200 (JSON with 3–5 fields).
- Latency: 50–250 ms on GPU for a 3–8B model; 300–800 ms CPU. Cache aggressively.

Mini model choices
- Open-source 3–8B instruct models (e.g., Llama-3.x-8B, Mistral-7B, Qwen2.5-7B) work well.
- If using provider “mini” endpoints, the smallest reasoning-capable tier usually suffices.
- For reranking, use a dedicated cross-encoder (e.g., bge-reranker, Cohere Rerank) after retrieval.

A minimal, production-style prompt (single-call builder)
System: You are a fast query builder for retrieval. Return strict JSON. No prose.
User:
Recent summary: User testing RAG on Jira/process docs; prefer Jira namespace if relevant.
Turn: how do I bulk edit jira issues to change assignee?

Return JSON with:
- route: one of ["chitchat","jira","code","docs","other"]
- standalone: self-contained query (<= 200 chars)
- paraphrases: 2–3 alternatives
- hyde: 1–2 sentence hypothetical answer to embed
- entities: list of salient terms (project names, features, file/classes, JQL terms)

Constraints:
- Keep it concise; avoid pronouns like “it/this.”
- If small-talk, route="chitchat".
- Use domain nouns from summary only to disambiguate; don’t copy the whole summary.

Example output
{
  "route": "jira",
  "standalone": "Steps to bulk edit issues and change assignee in Jira Cloud, including required permissions and limits.",
  "paraphrases": [
    "How to change assignee via Jira Cloud bulk edit, with prerequisites and constraints.",
    "Jira Cloud procedure to bulk change assignees and audit considerations."
  ],
  "hyde": "In Jira Cloud, go to Filters > View all issues, run a JQL, Tools > Bulk change, select issues, Edit, set Assignee, and confirm. Requires Bulk Change permission.",
  "entities": ["Jira Cloud", "bulk edit", "assignee", "permissions", "JQL"]
}

How it’s used
- Embed standalone + paraphrases (+ HyDE if you like) with query: prefix for E5/BGE.
- Use route to pick namespace (jira_kb vs docs).
- Feed entities as boosted terms for BM25/hybrid.

Practical tips
- Summarize history into 1–2 sentences; don’t embed the summary, only use it to rewrite.
- Add a confidence/score check: if top1 similarity < threshold, ask a clarifying question or skip RAG.
- Cache the builder’s JSON by normalized user turn + summary hash.
- Log route, standalone, and retrieval scores to tune thresholds.
- Keep HyDE optional; enable for short/underspecified queries.

If you want, share two of your real prompts and namespaces, and I’ll draft the exact builder prompt and sample JSON tuned to your corpora.
then I could make that also be the router?  like have the prompt return something like CHITCHAT instead of a new prompt?  something easy to scan for before trying the RAG lookups?
Yes. Make the same “query builder” also be the router. Return a tiny, scan-friendly field you can check before any retrieval.

What to return
- route: one of CHITCHAT, JIRA, CODE, DOCS, OTHER
- rag: boolean (false means “don’t retrieve”)
- standalone/paraphrases/hyde/entities: only populated if rag=true

Minimal JSON schema
{
  "route": "CHITCHAT|JIRA|CODE|DOCS|OTHER",
  "rag": true,
  "standalone": "…",
  "paraphrases": ["…"],
  "hyde": "…",
  "entities": ["…"],
  "reason": "short why",
  "confidence": 0.0
}

Prompt (single-call builder + router)
System: You are a fast query builder and router for RAG. Return strict JSON. No prose.
User:
Recent summary: {1–2 sentence history summary}
Turn: {user_text}

Rules:
- If small-talk/jokes/greetings/meta, set route="CHITCHAT" and rag=false. Leave standalone/paraphrases/hyde empty.
- If the turn is answerable from Jira/process docs, route="JIRA" and rag=true.
- Else choose CODE, DOCS, or OTHER. Keep standalone <= 200 chars. Avoid pronouns.

Return fields: route, rag, standalone, paraphrases, hyde, entities, reason, confidence.

Fast-path code
res = builder_llm(summary, turn)  # returns dict
if not res.get("rag", True) or res.get("route") == "CHITCHAT":
    return handle_without_rag(turn, route=res["route"])
# else proceed with RAG
queries = [res["standalone"], *res.get("paraphrases", [])]
# add prefixes like "query: " if needed, then embed + retrieve

Example outputs
- CHITCHAT case:
{
  "route": "CHITCHAT",
  "rag": false,
  "standalone": "",
  "paraphrases": [],
  "hyde": "",
  "entities": [],
  "reason": "small-talk/joke; retrieval not useful",
  "confidence": 0.94
}

- JIRA/RAG case:
{
  "route": "JIRA",
  "rag": true,
  "standalone": "Steps to bulk edit issues and change assignee in Jira Cloud, including permissions and limits.",
  "paraphrases": [
    "How to change assignee via Jira Cloud bulk edit, with prerequisites.",
    "Jira Cloud procedure to bulk change assignees and audit considerations."
  ],
  "hyde": "Use Filters > View all issues, run JQL, Tools > Bulk change, Edit > Assignee. Requires Bulk Change permission.",
  "entities": ["Jira Cloud","bulk edit","assignee","permissions"],
  "reason": "explicit Jira how-to",
  "confidence": 0.88
}

Tips
- Make rag=false the only condition to skip retrieval; route is for namespaces.
- Add a confidence threshold to fall back to a clarifying question when low.
- For maximum scan-speed, you can also add a top-level flag like mode: "NO_RAG" | "RAG".

------ASSISTANT------
That’s afantasticevolution — what you’ve designed here is basically acombined router + query-builder micro-LLM, which is exactly how mature RAG systems are moving now (e.g., OpenAI Assistants + ReAct hybrids).Here’s how you could capture it as a2.3.1 requirement blockso it fits neatly after 2.3 in your doc:Version 2.3.1 — RAG Query Builder + RouterPurposeAdd an intelligent pre-RAG “builder” stage
...[middle omitted for tagging]...
lder JSON.Routes and confidence values logged for every turn.Performance acceptable (≤ 800 ms avg builder latency).SummaryThis 2.3.1 upgrade gives Morpheus a proper“cognitive router”— a small meta-LLM that decides whether to retrieve and how to phrase the query.It cuts down on irrelevant RAG calls, improves retrieval quality, and 
lays the groundwork for the autonomous sleep/dream phases in v3.0+.

------CURRENT TURN------
------USER------
Dreaming_While_You_Sleep.txtDocumenthere is an example of what the pruned files in the RAG will look like.  is there anymore we can do in the query knowing that we have Topics and stuff identified in the pruned data?

------ASSISTANT------
Yes — since yourpruned RAG filesalready include structured metadata like#topics,#decisions, and#open_questions, you can use that information to make the retrieval queriesmuch smarterthan a plain text similarity search.Here’s how to take advantage of those fields directly in yourquery builder (2.3.1)or retrieval pipeline:1. Topic-Aware Query ExpansionWhen the builder model receives a user turn, expand the query by:Searching for overlapping or related#topicskeywords in the index metadata.Embedding both the original query and the top N topic terms.Running a hybrid retrieval where metadata topics get boosted weight (e.g.,score += 0.1 if topic overlap).Example:If the user asks “how does pruning work at
...[middle omitted for tagging]...
nd cut off by the adjusted score.6. Why It WorksThis aligns directly with how your pruned files are structured:#topics= high-level concept tags for recall.#decisions= key factual anchors.#open_questions= forward links for exploration.That makes your RAG effectively asemantic + symbolic hybrid memory, where metadata guides relevance, and embeddings handle nuance — precisely what your “Dreaming When You Sleep” design aims forDreaming_While_You_SleepDreaming_While_You_Sleep.In short:Yes — you can andshoulduse the#topicsmetadata to bias retrieval.The builder LLM can emit topic cues; the retriever can match and weight 
them; and the result is a RAG that “remembers by concept,” not just by 
surface text.


====== TAGGER RESPONSE ======
{
  "topics": "rag, query builder, retrieval, topics, metadata, hybrid search, Dreaming_While_You_Sleep",
  "intent": "ask for ways to improve retrieval using pruned file metadata",
  "type": "technical, design",
  "semantic_handle": "topic-aware rag query expansion",
  "questions": []
}
