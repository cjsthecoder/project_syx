1) Builder input and summary
Should the “recent summary” be an on-the-fly 1–2 sentence abstraction of the last N messages (how many pairs, or max tokens), or is there a separate summary we should keep updated?
make a summary of all the message pairs in memory that have not been rolled off to memory.  make it 3-4 sentences max.

2) Builder model and limits
Default BUILDER_MODEL=gpt-4o-mini is fine; confirm we’ll call it via the same OpenAI channel as chat. Any max latency/budget guardrails besides BUILDER_MAX_TOKENS=512 (e.g., timeout ms)?
yes, same OpenAI channel.  no other latency/budgets

3) Failure/timeout behavior
If the builder times out or returns invalid JSON, should we:
fallback to current V2.3 flow (no rewrite, normal daily+main retrieval), or
skip RAG and answer directly?
log a warning and just don't use RAG

4) “rag=false” handling
When rag=false or route=CHITCHAT, do we strictly bypass any retrieval and run chat normally (still including in-memory conversation history)?
yes, run chat normally

5) HYDE/paraphrases count
Target count for paraphrases and whether HYDE is always included or only when confidence < 1.0. Defaults OK at 2–3 paraphrases + 1 HYDE?
Yes — defaults are fine.

6) Topic/entity extraction → metadata
Main index metadata today only has filename, page_number. The spec references chunk #topics, #decisions, #open_questions.
Should we retrofit existing chunks with a sidecar metadata JSON keyed by docstore id, or re-index to embed these fields into the FAISS docstore metadata?
If sidecar, where to store and how to key (e.g., memory/{project}/faiss/meta_topics.json)?
Use a sidecar metadata JSON keyed by FAISS docstore ID.
Do not re-index everything right now — just extend metadata externally and merge the two sources at retrieval time.
Store the sidecar at memory/{project}/faiss/meta_topics.json.
Use a sidecar metadata JSON (memory/{project}/faiss/meta_topics.json) keyed by FAISS docstore ID. Avoid full re-indexing; merge metadata at retrieval time. This keeps updates cheap and flexible.

7) Namespaces/routing
For “jira”, “code”, “docs” namespaces: how should we map these in our current per-project index? Options:
File-based tags by directory (e.g., uploads/docs/, uploads/code/) → carry into metadata.
A new File.category column and tag chunks accordingly.
Or just a metadata-sidecar with file-to-namespace map. Which do you prefer?
Use a metadata-sidecar with a file-to-namespace map.
Don’t restructure directories or modify the DB schema right now.
Store the mapping in a simple JSON (e.g., memory/{project}/faiss/meta_namespaces.json).
Use a metadata-sidecar (memory/{project}/faiss/meta_namespaces.json) mapping files to namespaces. Avoid directory restructuring or DB changes; merge at retrieval for filtering/boosting.

8) Topic vector cache
Scope and storage: in-memory per project with LRU/TTL, or persisted to disk (e.g., memory/{project}/daily_topics_cache.json)? What eviction policy is acceptable?
Keep the topic vector cache in-memory per project with LRU + TTL eviction.
No need to persist it to disk yet — regenerate on demand.
If desired later, persist to memory/{project}/daily_topics_cache.json as an optional optimization.
Use in-memory per-project cache with LRU/TTL eviction (e.g., 24h, max 500 entries). No disk persistence needed now; regenerate embeddings if missing. Optionally persist later as memory/{project}/daily_topics_cache.json.

9) Reranking boosts
Confirm boosts are multiplicative (e.g., score = TOPIC_BOOST) and applied in this order: topic → decision → open_question → then normalize to [0..1] before truncation.
Any cap on total compounded boost (e.g., clamp to 1.0)?
Yes — that’s correct”

10) Deduping
We currently do exact-text dedupe; we can add near-duplicate dedupe with cosine ≥ DEDUPE_SIMILARITY_THRESHOLD. Confirm it should run after merging daily+main and before token budgeting.
yes, confirmed

11) Context budget order
Confirm final order remains:
Daily first up to DAILY_RAG_MAX_TOKENS
Main fills remaining up to RAG_CONTEXT_MAX_TOKENS
Sections labeled “Daily:” then “Main:”
yes, confirmed

12) Confidence threshold behavior
If confidence < BUILDER_CONFIDENCE_MIN, do we:
Ask a clarifying question (and pause the turn), or
Proceed with conservative retrieval (e.g., fewer paraphrases, skip HYDE), or
Skip RAG and answer directly?
Proceed with conservative retrieval — use only standalone query, skip HYDE/paraphrases, continue turn. Avoid clarifier interruption or full skip.

13) Caching builder output
For BUILDER_CACHE=True, cache key = hash(project_id, summary, user_turn) with a short TTL (e.g., 5–10 minutes)? OK to store in memory only?
Yes — store the builder’s JSON output in an in-memory cache, keyed by a short-lived hash like (project_id, summary, user_turn) with a 5–10 minute TTL.
No need for disk persistence.

14) Logging and visibility
“logs/builder.log”: rotate by size or date? Redact PII or full JSON OK? Also OK to add route + confidence to stats header temporarily for debugging?
yes, add extra debugging messages as needed.  but use the existing logger, don't make a new logging system

15) ENV additions
I will add BUILDER_MODEL, BUILDER_CONFIDENCE_MIN, BUILDER_MAX_TOKENS, BUILDER_CACHE, TOPIC_BOOST, DECISION_BOOST, QUESTION_BOOST to config.py and Makefile setup-env. Any other defaults you want to change from the spec?
no, that looks good

16) Placement and interfaces
I plan to add core/query_builder.py with:
build_query(history_summary: str, user_text: str) -> dict (validated against the JSON schema).
apply_topic_rerank(snippets, topics, entities, boosts).
Wire into chat_endpoint before retrieval.
Confirm this location and interface.
yes

17) Backward compatibility
If builder is disabled by ENV or fails, we’ll fall back to the current 2.3 retrieval path without topic reranking. OK?
yes



1) Clamp on boosted scores
After applying topic/decision/open_question boosts, should we clamp final score to 1.0 before sorting/truncation? I’ll default to clamp=1.0 unless you prefer another normalization.
Yes — clamp boosted scores to 1.0 before sorting/truncation.


2) Builder cache TTL and size
Exact TTL for builder JSON cache? OK to default to 10 minutes and cap at, say, 500 entries per process?
yes

3) Logging destination for builder
You said use the existing logger, not a new system. Should I:
Log builder records into the main rotating app log with marker type="builder" (no separate file)?
Or also create a dedicated file handler to logs/builder.log using the same logger config?

Put this at the top of any new python file, logger = logging.getLogger(__name__), and that is enough.  

4) Namespace routing behavior
When route yields a namespace (e.g., “jira”), should we hard-filter results to that namespace first and only fall back to full corpus if none, or always search full corpus but boost the namespace?
Boost the namespace first; fall back to full corpus, not a hard filter.
In other words — always search the full corpus, but strongly boost results from the routed namespace.


5) Sidecar meta reads/writes
I’ll place both sidecars at memory/{project}/faiss/ and guard with a daily.lock-style FileLock. OK to reuse the same lock file (e.g., faiss.lock) for meta reads/writes?
Yes, reusing a single faiss.lock file for both FAISS index and sidecar metadata reads/writes is acceptable — and actually preferable for simplicity.
Just make sure it’s a short-lived, well-scoped lock.

6) Unmapped namespace default
If a file isn’t listed in meta_namespaces.json, what default namespace should we assume (“other”)? And include “other” when route is unknown?
Default to "other" for unmapped files, and always include "other" when the route is unknown.

7) Low-confidence vs builder failure
Confirm difference:
Low confidence (< BUILDER_CONFIDENCE_MIN): conservative retrieval (standalone only; no HYDE/paraphrases).
Builder failure/timeout/invalid JSON: log warning and skip RAG entirely (answer directly).
Yes, confirmed