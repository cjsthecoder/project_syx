# Optional local overrides for tools/syx_memory_search.sh.
# SYX_REPO_DIR defaults to this repository when unset.
# SYX_BASE_URL defaults to http://127.0.0.1:8000 when unset.
SYX_AGENT_TOKEN="local-token"